<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Class="RentalApp.Views.BrowseItemsPage"
    Title="Browse Items">

    <CollectionView
        x:Name="ItemsCollection"
        ItemsSource="{Binding Items}"
        SelectionMode="Single"
        SelectionChanged="OnSelectionChanged">

        <CollectionView.Behaviors>
            <toolkit:EventToCommandBehavior
                EventName="SelectionChanged"
                Command="{Binding SelectItemCommand}"
                CommandParameter="{Binding Source={x:Reference ItemsCollection}, Path=SelectedItem}" />
        </CollectionView.Behaviors>

        <CollectionView.ItemTemplate>
            <DataTemplate>

                <!-- Root element changed to ContentView -->
                <ContentView x:Name="RootContainer"
                             BackgroundColor="Transparent">

                    <Border x:Name="ItemBorder"
                            Stroke="#ccc"
                            StrokeThickness="1"
                            Padding="10"
                            Margin="10"
                            BackgroundColor="White">

                        <VerticalStackLayout>
                            <Label Text="{Binding Title}"
                                   FontSize="18"
                                   FontAttributes="Bold" />

                            <Label Text="{Binding Description}"
                                   FontSize="14" />

                            <Label Text="{Binding Category}"
                                   FontSize="12"
                                   TextColor="Gray" />
                        </VerticalStackLayout>

                    </Border>

                    <!-- Purple highlight -->
                    <ContentView.Triggers>
                        <DataTrigger TargetType="ContentView"
                                     Binding="{Binding Source={x:Reference ItemsCollection}, Path=SelectedItem}"
                                     Value="{x:Reference ItemBorder}">
                            <Setter Property="BackgroundColor" Value="#E8D9FF" />
                        </DataTrigger>
                    </ContentView.Triggers>

                </ContentView>

            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>

</ContentPage>
